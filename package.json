{
  "name": "frontend-internals",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "site:dev": "turbo dev --filter=@frontend-internals/site",
    "site:build": "turbo build --filter=@frontend-internals/site"
  },
  "devDependencies": {
    "turbo": "^2.6.0"
  },
  "packageManager": "npm@11.6.0"
}
